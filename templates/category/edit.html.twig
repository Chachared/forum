{% extends 'base.html.twig' %}

{% block title %}Edition de la catégorie{% endblock %}

{% block body %}
<header>
    <div class="container-fluid">
        <div class="row">
            <div class="col-12 text-center mt-2">
                <h1>Edition de la catégorie</h1>
            </div>
            <div class="col-12 d-flex justify-content-center mt-2 mb-3">
                {% include 'parts/nav.html.twig' %}
            </div>
        </div>
    </div>
</header>
    <main>
    <div class="container">
    <div class="my-2">
    {{ include('category/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <a href="{{ path('app_category_index') }}" class="btn btn-dark rounded-pill mt-2">retour</a>
    {{ include('category/_delete_form.html.twig') }}
    </div>
{% endblock %}
