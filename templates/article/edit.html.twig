{% extends 'base.html.twig' %}

{% block title %}Edition de l'article{% endblock %}

{% block body %}
<header>
    <div class="container-fluid">
        <div class="row">
            <div class="col-12 text-center mt-2">
                <h1>Edition de l'article</h1>
            </div>
            <div class="col-12 d-flex justify-content-center mt-2 mb-3">
                {% include 'parts/nav.html.twig' %}
            </div>
        </div>
    </div>
</header>
    <main>
    <div class="container">

    {{ include('article/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_article_index') }}" class="btn btn-dark rounded-pill mt-2">Retour</a>

    {{ include('article/_delete_form.html.twig') }}
    </div>
    </main>
{% endblock %}
