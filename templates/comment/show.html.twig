{% extends 'base.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}


<main>
    <div class="container">
    <div class="col-12 text-center mt-2">
    <h1>Voir le commentaire</h1>
</div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ comment.date ? comment.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ comment.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_article_show', {'id': article.id}) }}">Retour</a>

    <a href="{{ path('app_comment_edit', {'id': comment.id}) }}">Editer</a>

    {{ include('comment/_delete_form.html.twig') }}

    </div>
</main>
{% endblock %}
